{
  "name": "Akutsu",
  "private": true,
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "pidusage": "^4.0.1"
  },
  "scripts": {
    "clean": "rm -rf dist node_modules bun.lock scripts.json scripty && echo 'Done.'",
    "dev": "bun run server.js",
    "package": "bun build ./server.js --compile --outfile scripty"
  }
}
